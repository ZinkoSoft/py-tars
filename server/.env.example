WHISPER_MODEL=small
DEVICE=auto
COMPUTE_TYPE=auto
# How often to emit partials from the server WS (ms)
PARTIAL_INTERVAL_MS=300
# Tail window of audio used to compute partials (seconds)
TAIL_WINDOW_SEC=6.0
# Where to store/download models inside the container
MODELS_DIR=/models
# Logging level
LOG_LEVEL=INFO
