# Memory Worker Configuration

# MQTT Configuration
MQTT_URL=mqtt://user:pass@localhost:1883

# Memory Storage
MEMORY_DIR=/data
MEMORY_FILE=memory.pickle.gz

# RAG Configuration
RAG_STRATEGY=hybrid
# Options: naive (vector only), hybrid (BM25 + vector + rerank)

MEMORY_TOP_K=5
# Default number of results to return

EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
# SentenceTransformer model for embeddings

# Optional: NPU acceleration (Orange Pi 5 Max with rknnlite)
# USE_NPU_EMBEDDER=false
# NPU_MODEL_PATH=/path/to/model.rknn

# Character/Persona Configuration
CHARACTER_NAME=TARS
# Character to load from CHARACTER_DIR

CHARACTER_DIR=/config/characters
# Directory containing character TOML files

# Optional: Reranking model for hybrid strategy
# RERANK_MODEL=flashrank
# Options: flashrank, None (no reranking)
