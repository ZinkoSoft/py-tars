# Copy to .env and edit values
MQTT_USER=tars
MQTT_PASS=change_me
MQTT_HOST=127.0.0.1
MQTT_PORT=1883

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Whisper model: tiny, base, small, medium (small recommended for Pi 5)
WHISPER_MODEL=tiny
